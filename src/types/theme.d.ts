namespace Config {
    type Theme = {
        colors: {
            primary: string,
            primaryLight: string,
            backgroundPrimary: string
        },
        typography: {
            fonts: {
                primary: {
                    light: string,
                    lightItalic: string,
                    regular: string,
                    regularItalic: string,
                    bold: string,
                    boldItalic: string
                }
            }
        }
    }
}